using UnityEngine;

namespace InGame.Pools.Buildables {
    public class BuildablesPool : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
