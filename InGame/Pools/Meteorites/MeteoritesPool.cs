using UnityEngine;

namespace InGame.Pools.Meteorites {
    public class MeteoritesPool : MonoBehaviour
    {
        // Start is called before the first frame update
        private void Start()
        {
        
        }

        // Update is called once per frame
        private void Update()
        {
        
        }
    }
}
