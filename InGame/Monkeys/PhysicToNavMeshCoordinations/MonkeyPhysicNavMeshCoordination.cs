using UnityEngine;

namespace InGame.Monkeys.PhysicToNavMeshCoordinations {
    public class MonkeyPhysicNavMeshCoordination : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
