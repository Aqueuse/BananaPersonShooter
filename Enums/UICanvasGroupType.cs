namespace Enums {
    public enum UICanvasGroupType {
        HOME_MENU,
        GAME_MENU,
        LOAD,
        OPTIONS,
        BANANAPEDIA,
        CREDITS,
        BUILDSTATION,
        DEATH,
        HUD,
        INVENTORY,
        STATISTICS,
        TUTORIALS
    }
}
